schema_version: 1

context:
  name: elevenlabs
  version: "2.58.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/elevenlabs-${{ version }}.tar.gz
  sha256: 3281f0f6271dd8a40466a2d7cdd4bc3721937109fc2917248e87dbde3b7832a7

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - poetry-core
    - pip
  run:
    - python >=${{ python_min }}
    - httpx >=0.21.2
    - pydantic >=1.9.2
    - pydantic-core >=2.18.2
    - requests >=2.20
    - typing_extensions >=4.0.0
    - websockets >=13.0
  run_constraints:
    - pyaudio >=0.2.14

tests:
  - python:
      imports:
        - elevenlabs
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  homepage: https://elevenlabs.io/docs/api-reference/introduction
  repository: https://github.com/elevenlabs/elevenlabs-python
  summary: The official Python SDK for ElevenLabs.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - xhochy
